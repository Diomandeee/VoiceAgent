# Version Bump — Manual trigger
# Auto-generated by openclaw-ios-bootstrap.sh

name: Version Bump

on:
  workflow_dispatch:
    inputs:
      bump-type:
        description: 'Bump type'
        required: true
        type: choice
        options:
          - patch
          - minor
          - major
        default: patch

jobs:
  bump:
    uses: Diomandeee/.github/.github/workflows/reusable-version-bump.yml@main
    with:
      bump-type: ${{ inputs.bump-type }}
